# Partner Network Consolidation Issue Register

Scope: P7 → P10 on `feat/super-admin-partner-network-consolidation` from `origin/main` `f64e67fbfd9e8b5a5b647dd78265ada4478b485d`.

| ID | Severity | Source | Reproduction / impact | Repair | Proof | Status |
| --- | --- | --- | --- | --- | --- | --- |
| PNC-001 | HIGH | Hostile review P7 F1 | A cert beyond the normal 200-row queue cap did not open from `/admin/staff?certId=`. | Added a certId scope to existing R1 queue read; the Staff client sends it and admits only `pending_review`. | Hostile re-review confirmed the scoped existing R1 route removes the cap failure. Runtime execution remains blocked by absent dependencies. | PROVEN — P7 closed |
| PNC-002 | HIGH | Hostile review P7 F2 | P5 proof set did not cover enough server readiness branches. | Restored exhaustive pure and renderer proof and added a real-PostgreSQL collection test against the current migration lineage plus 0045, without P2/P6 migrations. | Hostile re-review confirmed all six dimensions, UNKNOWN and audience/action coverage, plus real service collection and no wallet write. | PROVEN — P7 closed |
| PNC-003 | HIGH | Hostile review P7 F3 | Canonical routes lacked visible top-level Partner Network navigation. | Added Overview, Partners, Stations, Infrastructure, and Settings navigation and an R3 read-only network stations page. | Hostile re-review confirmed the visible canonical IA and valid station deep-link route. | PROVEN — P7 closed |
| PNC-004 | MEDIUM | Hostile review P7 M1 | Partner filter could fall into the legacy queue response when status was omitted. | Partner/cert scoped queue calls now always take the rich existing queue branch. | Hostile re-review confirmed the guarded rich branch. | PROVEN — P7 closed |
| PNC-005 | MEDIUM | Hostile review P7 M2 | Malformed certId direct link was not explained. | Normal Staff queue remains visible and a plain invalid-id message is shown. | Hostile re-review confirmed the fail-safe message. | PROVEN — P7 closed |
| PNC-006 | HIGH | P8 hostile review | Canonical Partner workspace tabs were not all direct routes, fleet lifecycle controls needed to remain outside the network-wide view, and the first implementation hid retained legacy tabs with the flag off. | Added static canonical workspace routes, route-aware tab navigation, local malformed-ID fail-safe, a Partner-scoped station action dialog using existing step-up-protected fleet mutations, and explicit legacy tab render conditions. | Hostile re-review confirmed direct canonical routes, network read-only containment, and restored legacy Overview/Users/Locations/Activity/Audit reachability. Runtime execution remains blocked because this fresh worktree has no `vitest`/`tsc`. | PROVEN — P8 closed |
| PNC-007 | HIGH | P9 implementation review | Canonical Overview previously composed its network state through multiple browser reads, obscuring an R2 bounded projection and making operational deep links inconsistent. | Added `GET /api/super-admin/partner-dashboard` within the existing guarded router; it returns a bounded set-based projection, and the canonical page calls only that route. | R2 source contract test; focused runtime execution blocked because this fresh worktree has no `vitest`/`tsc`. | FIXED — hostile review pending |
