node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Secrets / environment
.env
.env.*
*.pem
*.key

# Replit internals
.replit
.upm
.local
replit.nix
.cache/
.config/

# Attached assets / uploads (binary, large files — docs copied to /docs instead)
attached_assets/
uploads/

# Unrelated scripts (not part of MintVault)
ebay_sniper.py
telegram_test.py
__pycache__/
*.pyc
sniper.log
sniper_stats.json
seen_ids.json
seen_items.txt
seen.json
seen_rss_ids.json
sold_cache.json
sold_cache_simple.json
/data/

# Logs
*.log

# Local script outputs (snapshots, dumps, etc.)
scripts/_snapshots/

# One-off debug / probe / investigation scripts — convention is to prefix
# the filename with `_` so they're easy to spot and easy to ignore.
scripts/_*
server/scripts/

# Standalone Electron app for Cricut label printing — separate concern, not
# part of the main MintVault build/import graph.
scripts/cricut-app/

# Backup files
*.bak

# Local-only working files (per-developer)
.claude/skills/cornelius-execution-style/
client/public/images/vault-club-room.webp
docs/v521-audit.md

# OS
Thumbs.db
.claude/worktrees/

# controlled-code-lead: local approval records + session locks (never committed)
.claude/controlled-code-lead/approvals/*.record.md
.claude/controlled-code-lead/approvals/*.record.json
.claude/controlled-code-lead/locks/
