# Engineering proof ledger — MintVault Scanner SOL campaign

| Claim | Source of truth | Proof | SHA | Dependencies / invalidation | Status |
|---|---|---|---|---|---|
| WP0 isolated seed is immutable and contains requested lineage through P9 | Git object graph | `git cat-file`, ancestry checks, A9 report; seed `d44a2c5363e702bb5aeb54157d7ad6a2af30546c` | `d44a2c53` | Invalidated if branch base changes | PROVEN |
| Active Partner worktree was not modified | Partner Git status before/after reviewer inspection | A9 read-only command log; exact dirty inventory retained | `d44a2c53` + dirty WIP | Recheck if Partner HEAD/status moves | PROVEN |
| Engineering OS is installed and campaign preflight is HOSTILE | Installed OS self-check and preflight JSON | `engineering check --self`; preflight risk `CRITICAL`, mode/required `HOSTILE` | `d44a2c53` + governance WIP | Re-run after manifest/governance change | PROVEN |
| Graphify repository intelligence matches the seed | Generated code graph metadata | Graphify 0.9.39; 11,275 nodes, 25,295 edges; source commit `d44a2c53` | `d44a2c53` | Any structural source change requires graph update | PROVEN |
| Existing native capture helper is runtime-compiled | Authoritative scanner source | `lide400-controller.js` `ensureBridge()` invokes `/usr/bin/xcrun clang` | `d44a2c53` | Invalidated when controller/helper packaging changes | PROVEN |
| Existing station private key can enter Electron main memory and App Support ciphertext | Authoritative scanner source | `station-identity.js` generates/exports PKCS8 Ed25519 and wraps whole identity with `safeStorage` | `d44a2c53` | Invalidated by identity-helper migration | PROVEN |
| Existing targeted queue is plaintext JSON and TIFF plaintext precedes durable encryption | Authoritative scanner source | `watcher.js` uses `targeted-capture-queue.json` and filesystem TIFF paths | `d44a2c53` | Invalidated by encrypted-queue implementation | PROVEN |
| P14 is not frozen | Partner source/Git | A9: durable evidence ends P9; 9 modified + 3 untracked P10-style files; required P14 docs absent | `d44a2c53` + dirty WIP | Recheck final Partner HEAD | PROVEN |
| Partner isolation boundary detects concurrent drift | Partner Git read-only snapshots | Partner moved from `d44a2c53` to `73b2072e` during WP0 and dirty inventory changed; no files copied or modified | `73b2072e` observed | Recheck only at frozen P14 gate | PROVEN |
| WP0 governance remains loaded and self-protected | Repository governance suite | `.claude/governance-tests/run-all.sh`: 4 suites passed, 0 failed | WP0 WIP | Governance file changes invalidate | PROVEN |
| WP0 source baseline builds and typechecks | Real repository commands | `npm run check` pass; lint 0 errors; `npm run build` pass | `d44a2c53` + WP0 governance | Application/dependency changes invalidate | PROVEN |
| Existing protected grading behavior was not changed by WP0 | Real targeted protected tests | MVGS/label targeted set 259 passed / 2 skipped after locked canvas rebuild; cold font rerun 11/11 | `d44a2c53` + WP0 governance | Protected source/dependency/font changes invalidate | PROVEN |
| Existing Scanner/Partner foundations execute non-vacuously | Real unit and disposable-Postgres tests | 119 passed / 1 skipped plus serial NEW/FIX/per-card-credit 76 passed / 0 failed | `d44a2c53` + WP0 governance | Scanner/Partner source or migrations invalidate | PROVEN |
| WP1 capture helper is built before packaging, never on a station | Build output plus runtime source boundary | WP1-01: Apple Silicon build produced one arm64 Mach-O at minOS 12.0; WP1-06: controller contains no `xcrun`, compiler, source or App Support executable fallback | WP1 WIP | Native/controller/build-script change invalidates | PROVEN |
| Runtime rejects capture-helper replacement before spawn | Executable hostile unit matrix | WP1-03: 15/15 pass covering symlink, bytes, mode, architecture, minOS, signature, identifier, Team ID, ad-hoc production and response protocol | WP1 WIP | Integrity module/manifest/runtime wiring change invalidates | PROVEN |
| Real native helper and controller execute the frozen versioned protocol | arm64 helper execution on current macOS | WP1-02: signed helper returned structured `disconnected`, protocol 1/helper 1.0.0; controller accepted it after real integrity verification | WP1 WIP | Hardware not attached; does not prove physical capture | PROVEN_LOCAL |
| Existing Scanner behavior remains green after WP1 | Scanner app and root targeted suites | WP1-04: Scanner 50/50; WP1-05: helper/profile 10/10; obsolete recovery assertion repaired to the existing target-bound FIX contract | WP1 WIP | Scanner/native/UI change invalidates | PROVEN |
| WP1 repository gates remain green | Governance/typecheck/lint/build/diff checks | WP1-07: governance 4/4, TypeScript pass, lint 0 errors, production build pass, `git diff --check` pass | WP1 WIP | Source/governance/dependency change invalidates | PROVEN |
| WP2 station private authority is helper-owned and SE-bound | Native source plus real Secure Enclave lifecycle | WP2-01: arm64 helper create/bind/sign/reload/retire; Ed25519 signatures verified; no production namespace touched | WP2 WIP | Final production access-group/signing proof requires WP6 package | PROVEN_LOCAL |
| WP2 migration preserves the legacy identity before retirement | Real test namespace migration and JS proof order | WP2-02: same fingerprint/install ID/nonce, valid possession proof, same-key convergence, substituted key rejection; v1 unlink after proof only | WP2 WIP | Legacy/package source changes invalidate | PROVEN_LOCAL |
| WP2 identity lookup does not silently create or cross namespaces | Helper state/source/real lifecycle | WP2-03: repeated status stays `ABSENT_NEW`, duplicate create rejected, corrupt/drift states fail closed, ad-hoc direct production access rejected | WP2 WIP | Signed-package namespace attributes still require WP6 proof | PROVEN_LOCAL |
| WP2 production SE/Keychain contract is source-enforced | Native Security.framework implementation and hostile re-review | WP2-04: permanent tagged SE `SecKey`, exact Team access group, ThisDeviceOnly/non-sync, entitlement failure closed; A3 final CLEAN | WP2 WIP | Developer-ID entitlements and cross-Mac test external | PROVEN_SOURCE |
| WP2 station-signed HTTP exchanges cannot overtake | Shared queue behavioral test and call-site inspection | WP2-05: forced first-request stall prevents second signature/network start; rejection recovery; A4 call-site review | WP2 WIP | Any new signed HTTP client invalidates | PROVEN |
| WP2 replay recovery scaffold cannot roll sequence backward | Canonical fixtures and real helper lifecycle | WP2-06: v1 compatibility; v2/op-ID/resync domains; same/replayed epoch rejected, strict newer epoch accepted | WP2 WIP | Scaffold remains inactive until P14 server reconciliation | PROVEN_LOCAL |
| WP2 semantic operations survive process restart with exact scope/payload | Durable-store/coordinator tests | WP2-07: fsync-before-return, same/same replay, payload/type conflict, tamper fail-closed, immutable completion | WP2 WIP | Store/coordinator changes invalidate | PROVEN |
| Paid NEW retains one ID and original payload across ambiguous outcomes | Restart coordinator plus disposition tests | WP2-08: restart/UI drift uses same UUID/payload; 502/503/504/malformed 2xx remain pending; only validated job or explicit unspent refusal closes | WP2 WIP | Main NEW/server contract changes invalidate | PROVEN |
| Enrolment client retains exact request across response loss | Enrolment coordinator tests and A4 final review | WP2-09: upgrade/location UI drift replays stored request; identity mismatch fails closed; no non-success (including current 400 key conflict) discards pending state; P14 server persistence pending | WP2 WIP | End-to-end proof blocked on P14 server contract | PROVEN_LOCAL |
| Production helper cannot be a direct same-user signing oracle | Native caller authentication and hostile review | WP2-10: signed self, dynamic parent PID, exact app ID/Team/designated requirement; dev production call rejected; A3 final CLEAN | WP2 WIP | Real signed-parent execution requires WP6/R-3 | PROVEN_SOURCE |
| Packaged helper trust pins MintVault Team, not pairwise equality | Hostile unit matrix | WP2-11: same attacker Team on app/helper rejected; missing/wrong pin fails; exact pinned Team succeeds in verifier fixture | WP2 WIP | Final Team value and Developer-ID signature require R-3 | PROVEN |
| Active station evidence never uses unsigned-body multipart | Behavioral regression and A4 review | WP2-12: active signed `uploadPair` refused 426 before I/O; staged JSON/digest path retained; legacy fallback remains explicit non-production only | WP2 WIP | Upload/client auth changes invalidate | PROVEN |
| Scanner starts no new physical action from stale local authority | Pure state tests plus main IPC source boundary | WP3-01: current session/MFA/permission/station/version is fetched before NEW, FIX, Scan, placement Preview/apply and Rescan; denial precedes watcher access; live 20–30s projection closes UI | WP3 WIP | Final status taxonomy/session family requires P14 reconciliation | PROVEN_LOCAL |
| SHIFT CHANGE is immediate and preserves device/evidence identity | Async ordering and source tests | WP3-02: local human credential clears before a deliberately stalled logout request; offline logout still locks locally; no capture/upload blocker; station identity is untouched | WP3 WIP | Server-side max residual bearer life requires 15m station-bound family | PROVEN_LOCAL |
| MFA enrolment and least privilege fail closed in Scanner first run | Pure classification and Scanner regression | WP3-03: enrolment-required differs from code challenge; enrolled operation requires `partner.cards.scan`, new enrolment requires `partner.stations.enrol`; missing location/role/offline/degraded states lock UI | WP3 WIP | Final end-to-end P14 auth tests remain | PROVEN_LOCAL |
| Durable capture bytes are encrypted and device-bound before ambiguity | Queue implementation plus fault/corruption tests | WP5-01: AES-256-GCM unique nonce; full authenticated metadata; HMAC index; Secure-Enclave-helper wrapped DEK; clone/tamper/missing-key/path-substitution failure; atomic fsync and private modes | WP5 WIP | Signed-package/cross-Mac proof remains WP6/WP9 | PROVEN_LOCAL |
| No abandoned capture plaintext silently survives restart | Startup sweep and executable tests | WP5-02: TIFF/JPEG in inbox, rejected, staging, scratch, processed, failed and discarded are unlinked as known scratch or encrypted into terminal quarantine; app directories repaired to 0700 | WP5 WIP | New capture directories or recovery paths invalidate | PROVEN |
| Upload attempts carry no durable authority and bind the exact evidence tuple | Client/watcher behavioral tests | WP5-03: fresh direct grant on every attempt; no URL persisted; station-only recovery excludes human token; grant/finalise bind full authority, provenance, digest and non-authoritative device timestamp | WP5 WIP | Final server implementation requires frozen P14 | PROVEN_LOCAL |
| Only a canonical tuple-exact server disposition resolves local evidence | State-machine and fault-injection tests | WP5-04: empty/malformed/mismatched 2xx remains NEEDS_RECONCILIATION; terminal non-acceptance quarantines; ACCEPTED is a restart-finalized deletion journal; local outcomes never fabricate dispositions | WP5 WIP | Final server response schema requires frozen P14 | PROVEN_LOCAL |
| Rescan and restart transitions do not expose a no-live-target gap | Atomic queue replacement and injected-write failure | WP5-05: old QUARANTINED plus fresh higher-revision PENDING target share one authenticated atomic index write; injected pre-commit crash retains old live target; injected ACCEPTED resolution crash converges on startup | WP5 WIP | Queue/index protocol changes invalidate | PROVEN |
| WP5 Scanner and repository regressions are non-vacuous | Real native builds and test/build commands | WP5-06: Scanner 99 pass/3 opt-in skips; opt-in SE 3/3; final queue slice 50/50; root focused 96 pass/3 environment skips; governance 4/4; type/build pass; lint 0 errors; native arm64 helpers verify | WP5 WIP | Source/dependency/governance changes invalidate | PROVEN_LOCAL |
| WP6 emits one installable branded arm64 application and canonical distribution set | Real electron-builder output on Apple Silicon | Clean-source app, DMG, ZIP, `latest-mac.yml`, `SHA256SUMS` and MintVault manifest generated; exact app ID/name/icon, 19 arm64 Mach-O files and macOS 12.0 floor verified | `03053c3c` | Final Developer-ID/notary proof is R-3; rebuild after source changes | PROVEN_LOCAL |
| WP6 package contains runtime only and its dependencies actually load | ASAR/unpacked inventory plus packaged Electron execution | Sharp native unpack set is exact; no native/build source matched; all declared production entrypoints exist and import through the app executable, including ESM `node-fetch/src/index.js`; Scanner audit reports 0 vulnerabilities | `03053c3c` | Dependency/config/allowlist change invalidates | PROVEN_LOCAL |
| WP6 native/package trust is source- and Team-bound | Preparation nonce, generated pins, helper manifests and verifier tests | Clean source `03053c3c` is embedded in both helpers; JS Team pin is signed in ASAR, Swift pin compiled into identity helper; same-attacker trust replacement is rejected | `03053c3c` | Exact owner Team and real Developer-ID signatures remain R-3 | PROVEN_SOURCE |
| WP6 ZIP and DMG independently contain the same verified build | Independent post-finalisation reopen and cross-binding | ZIP and read-only-mounted DMG each verify 19 Mach-O files, source `03053c3c`, helper digests and bundle digest `781bdd6a…1e025`; final artifact SHA-256 values are DMG `42e09efc…a28cc` and ZIP `ae95e3e0…8aa0e` | `03053c3c` | Any artifact-byte/source/package change invalidates | PROVEN_LOCAL |
| WP6 release workflow fails closed before Apple authority use | Workflow source, YAML parse and hostile review | Full-SHA actions, Apple-Silicon assertion, minimal permissions, protected environment, approved main/exact source-SHA gate before checkout/secrets, one credential set, credential cleanup before upload; A7 final review CLEAN | `03053c3c` | Workflow/action/source-policy change invalidates | PROVEN_SOURCE |
| WP6 Scanner regression and package contracts are non-vacuous | Node test runner, audit, syntax/YAML/diff gates | Scanner 112 passed / 3 explicit Secure-Enclave skips; focused package/helper 30/30; zero Scanner audit vulnerabilities; governance 4/4, type/build pass, lint 0 errors/2,743 warnings | `03053c3c` | Source/dependency/workflow change invalidates | PROVEN_LOCAL |
| WP7 static metadata cannot select update or rollback authority | Authenticated policy validator and hostile feed matrix | Missing policy, different newer release, withdrawn old signed release, wrong direction/Team/source/hash/lifetime all fail before download; explicit rollback requires exact authenticated `ROLLBACK` target | `48c843a2` | Final policy route/schema must reconcile with frozen P14 | PROVEN_LOCAL |
| WP7 real MacUpdater path and candidate lifecycle are fail-closed | Pinned library source plus behavioral fake/cache mutation tests | Pinned MacUpdater's empty return is reproduced; `update-downloaded.downloadedFile` is exact-path/hash bound; new checks invalidate old candidates and immediate pre-quit rehash rejects mutation | `48c843a2` | Electron/updater/version change invalidates | PROVEN_LOCAL |
| WP7 update transport is byte- and resource-bounded | Real streaming helpers through pinned updater fake plus overflow/timeout/symlink tests | Exact policy sizes/hashes are enforced during latest/ZIP/DMG transfer; manifest/checksum bodies have fixed maxima; oversized, stalled, cancelled or pre-existing-path writes fail before install and the 2 GiB/5% capture reserve remains free; removing the exact streaming ceiling made the direct limiter test RED and restoring it made GREEN | `48c843a2` | Fetch/updater/policy/storage change invalidates | PROVEN_LOCAL |
| WP7 DMG recovery never falls back to an unverified feed page | Private bounded download and open-boundary tests | Exact policy-bound DMG is streamed with exclusive creation into a non-symlink 0700 cache, size/SHA-256 checked, then policy/bytes rechecked immediately before local open; absent policy fails closed | `48c843a2` | Physical Gatekeeper/install proof remains R-3/WP9 | PROVEN_LOCAL |
| WP7 update restart preserves evidence custody | Watcher/main state, stalled-recovery and asynchronous-install tests | A synchronous install quiesce precedes MacUpdater; authority awaits, polls, maintenance and all watcher entry points recheck it; restart remains denied for startup/delayed-inbox/preview scratch and ordinary physical work, and only explicit updater failure releases the latch; removing recovery-work participation made two held-open recovery tests RED and restoring it made GREEN; A5 final targeted re-review CLEAN | `48c843a2` | Watcher/updater state or native MacUpdater behavior invalidates | PROVEN |
| WP7 login startup is post-enrolment and non-coercive | Login-item unit/source tests | Unpackaged/install/pre-enrolment performs no action; first device-bound enrolment requests main-app login once; persisted marker prevents overwriting a later user choice; package carries no service agent | `48c843a2` | Login/state/package config changes invalidate | PROVEN_LOCAL |
| WP7 clean package preserves the verified updater/login runtime | Real Apple-Silicon package and independent ZIP/DMG reopen | Clean source `48c843a2`; 19 arm64 Mach-O files; ZIP and DMG match bundle digest `2b6d3058…ef64` and capture/identity hashes `af9dce03…f29f` / `2c797bd1…70f89`; local manifest is explicitly `releaseReady:false` | `48c843a2` | Any Scanner source/package/dependency or Apple-signing change invalidates | PROVEN_LOCAL |
| WP7 Scanner regression and dependency audit are non-vacuous | Node test runner, focused policy suite, npm audit and repository gates | Scanner 136 passed / 3 explicit Secure-Enclave skips; focused updater/login suite 24/24; zero Scanner audit vulnerabilities across 327 dependencies; governance 4/4, TypeScript/build pass, lint 0 errors/2,767 existing warnings, syntax and diff whitespace pass | `48c843a2` | Any Scanner source/dependency/governance change invalidates | PROVEN_LOCAL |
| WP8 locked profile is device-bound, immutable and restart-convergent | AES-GCM profile store, native key-wrapper contract and durable acceptance tests | UUIDv4 helper key ID; station/revision AAD; private atomic files; response loss replays one exact operation; conflicts/legacy acknowledgements/local-commit failure fail closed; current profile is saved before pending cleanup | WP8 WIP | Final server persistence/response requires frozen P14 | PROVEN_LOCAL |
| WP8 ACTIVE requires the exact accepted hardware/profile proof | Controller/native helper, TIFF analysis and live authority tests | Exact 100×130 mm ROI, 1200-DPI ±2-pixel RGB 8-bit TIFF, matching helper/hardware, nonblank complete card and safety margins; local/server revision and digest must match before ACTIVE/capture | WP8 WIP | Real LiDE 400 and signed-package execution remain physical/external | PROVEN_LOCAL |
| WP8 appliance surface is least-privilege and recoverable | Renderer source/unit tests plus in-app browser harness | Guided Account→MFA→Station→Profile→Ready states; underlying UI inert/aria-hidden; normal settings locked; SHIFT CHANGE accessible; service-only `profile_check` re-verification while physical work stays denied | WP8 WIP | Browser harness is local; staging accessibility/operator proof remains WP11 | PROVEN_LOCAL |
| WP8 Scanner regression and hostile review are non-vacuous | Node runner, native helper verification, syntax/diff checks and A6 review | Full Scanner 151 passed / 3 explicit Secure-Enclave skips; focused A6 61/61; arm64/minOS-12 helper 1.0.1 rebuilt and sealed locally; zero Scanner audit vulnerabilities; A6 final targeted review CLEAN | WP8 WIP | Clean-source package/regression checkpoint follows commit | PROVEN_LOCAL |
| WP9 hostile security review exhausted the mandated Scanner attack surface | Independent A8 hostile passes plus repair/re-review loop | A8 final verdict CLEAN after exercising auth/session, station forgery, identity/replay/idempotency, evidence/operator/profile substitution, upload grants, updater, helper replacement/direct launch, renderer compromise, queue/control tamper and legacy runtime paths; no Scanner-owned BLOCKER/HIGH remains | WP9 WIP | Partner wire, Apple release and physical/staging gates remain separate | PROVEN |
| Terminal identity retirement converges across every native interruption | Real helper process-exit injection plus JS coordinator tests | Native CREATE/MIGRATE/RETIRE journals recover every injected phase; RETIRE globally denies capability use; cold restart compares exact raw custody without unwrap; exact fingerprint retirement then fsync-cleans identity/session/state/semantic/profile/queue namespace | WP9 WIP | Native/helper/coordinator/custody changes invalidate | PROVEN_LOCAL |
| Semantic mutation authority cannot be forged, deleted or rolled back locally | MAC/high-water behavioral and snapshot tests | Complete ledger is helper-key authenticated; pending NEW/FIX/CANCEL/enrolment survives response loss/restart; recomputed digest, lifecycle flip, deletion and prior-valid snapshot restoration fail closed | WP9 WIP | Final server-side scope/result registry requires WP10 | PROVEN_LOCAL |
| Capture bytes remain one attested object from helper through encrypted custody | Native attestation, stable-descriptor and pathname-swap tests | Capture/profile proof binds helper digest+size; metadata/frame/hash/Preview/encryption consume one `O_NOFOLLOW` descriptor; swaps, truncation, extension, sparse files and index/AAD substitution fail before authority | WP9 WIP | Physical LiDE 400 capture remains external | PROVEN_LOCAL |
| Packaged renderer, network, storage and helper boundaries fail closed | Containment/runtime-path/origin/helper tests and fresh ASAR comparison | Exact local main-frame IPC, sandbox/CSP/no network/navigation, path-free projections, immutable packaged API/account roots, no redirects, root-owned release helper tree and signed self/parent checks; 18 changed runtime files byte-match source | WP9 WIP | Real Developer-ID installation authority is R-3 | PROVEN_SOURCE |
| Full WP9 Scanner regression is non-vacuous | Node test runner plus real native opt-in suite | 225 total: 220 pass, 0 fail, 5 explicit native opt-in skips; the skipped Secure-Enclave/interruption suite was executed separately 5/5 on this Apple-Silicon Mac; `git diff --check` clean | WP9 WIP | Scanner source/test/native changes invalidate | PROVEN_LOCAL |
| Fresh WP9 local package and distribution artifacts match current source | electron-builder, packaged-runtime import, independent ZIP/DMG reopen | Local structural app/DMG/ZIP/latest/checksums/manifest pass; 19 Mach-O files; source commit `b0986b48`, preparation `f853a709`; bundle digest `3c79bfef…7723`; DMG `24d5b8c7…170a`; ZIP `f4ee57be…9a1f`; exact helper manifests/digests and all changed ASAR files match source | WP9 WIP | Explicitly `releaseReady:false`; Developer-ID/notary/Gatekeeper release proof remains R-3 | PROVEN_LOCAL |
| WP10 reconciles against the authoritative frozen Partner P14 HEAD | Git ancestry plus semantic merge and controlled read-only Partner review | Frozen Partner P14 `e4d3bf5d` is incorporated by merge commit `4018116d`; the active Partner worktree was not modified; final Scanner/Partner authority work is additive in migration 0091 and current routes/services | WP10 WIP | A newer owner-declared Partner authority would require a fresh reconciliation | PROVEN |
| Station protocol v2 and recovery are server-authoritative | Real PostgreSQL 17 migration and behavioral test | Global sequence/credential/request epochs reject replay; one-time signed challenge advances the epoch and rejects reuse/old epoch; global semantic IDs bind actor/station/tenant/location/endpoint/type/payload/result and cross-tenant reuse conflicts | WP10 WIP | Migration/identity/semantic registry changes invalidate | PROVEN |
| Scanner human sessions are short, station-bound and idle-correct | Real PostgreSQL 17 session-family test plus Scanner client regression | Fresh-MFA Scanner login mints 15-minute access; exact signed station binds 12-hour refresh; another station cannot refresh; explicit activity touches idle while background session/enrolment/heartbeat does not; SHIFT CHANGE revokes access/refresh | WP10 WIP | Auth/session/middleware/station-client changes invalidate | PROVEN |
| Final capture/profile/disposition authority is tuple-exact | Migration, evidence-binding unit tests and real station authority suite | Immutable profile revision/digest, capture authorisation/semantic/job/operator/purpose/server-lifetime tuple, staging grant and final disposition are cross-checked; Rescan changes only allowed revision/IDs and CANCEL releases one reservation exactly once | WP10 WIP | Capture/profile/finalisation schema or routes invalidate | PROVEN |
| Replacement and transfer preserve paid lineage without inherited authority | Real PostgreSQL 17 replacement/transfer proof | Replacement atomically revokes old and activates new identity; accepted FRONT remains current; independently-profiled replacement gets only missing BACK on same job/MV/certificate with unchanged ledger/job counts; cross-tenant transfer and unresolved custody are refused | WP10 WIP | Fleet/capture/credit/location changes invalidate | PROVEN |
| WP10 Scanner and Partner matrices are fully green | Node Scanner runner and hermetic Partner suite matrix | Scanner: 226 total, 221 pass, 0 fail, 5 explicit native-hardware skips; Partner: all 37 suites green including six real station-v2 authority tests | WP10 WIP | Scanner/Partner source, dependency or migration changes invalidate | PROVEN_LOCAL |
| WP11 credential-independent staging rehearsal is complete locally | Broad repository run plus disposable PostgreSQL 17 prerequisites | Broad run: 4,644 pass with only five explicit DB-environment abort files; those five were rerun on safety-labelled `mintvault_vq_phase10_local` and passed 62/62. No external staging, hardware or production state was touched. | WP10 WIP | External staging rehearsal still requires owner credentials/permission and physical Macs | PROVEN_LOCAL |
| WP12 source gates pass on the reconciled candidate | ESLint, TypeScript, production build and whitespace gates | `npm run lint` exits 0 (repository baseline warnings only), `npm run check` exits 0, `npm run build` exits 0, `git diff --check` exits 0 | `478477fc` | Any code/governance change invalidates | PROVEN_LOCAL |
| WP12 final local package matches the frozen source candidate | Clean-source electron-builder, independent app/ZIP/DMG reopen and dependency audit | Source `478477fc49505dbf254058af4084941c4017a540`, preparation `44dbd88a…85c0`, 19 arm64 Mach-O files, bundle digest `51973594…13945`; capture helper `0e505746…ad75b`, identity helper `38ee4e61…6e041`; DMG `2a00496e…f861b`, ZIP `5d8ba5e2…6a9cc`; Scanner production audit reports 0 vulnerabilities | `478477fc` | Correctly `releaseReady:false`; Developer-ID/notary/staple/Gatekeeper and physical Pilot proof remain R-3/R-10 external | PROVEN_LOCAL |
