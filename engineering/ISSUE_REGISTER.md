# Engineering issue register

| ID           | Severity | Source                              | Reproduction                                                                                                                                                                                                | Reachability                                                | Impact                                                               | Repair                                                                                                                                                                   | Proof                                                                                                                                                                    | Status |
| ------------ | -------- | ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- | -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| GOV-001      | MEDIUM   | Graphify governance test            | Run `bash .claude/governance-tests/test-graphify-integration.sh` before the repair                                                                                                                          | Always                                                      | The test could not verify the required `--code-only` privacy command | Pass a literal pattern separator to `grep`                                                                                                                               | The repaired test passes                                                                                                                                                 | PROVEN |
| GOV-002      | HIGH     | Engineering OS anti-drift check     | Let the repository formatter process managed AGENTS/CLAUDE blocks, then run `engineering check`                                                                                                             | Commit-time formatting                                      | Managed-block drift blocks Engineering OS verification               | Restore blocks with `engineering upgrade`; avoid the formatter for managed-block-only repairs                                                                            | `engineering check` passed after repair                                                                                                                                  | PROVEN |
| SCN-LINT-001 | HIGH     | Final reconciliation baseline audit | The earlier Engineering OS enrolment candidate reported six `no-unreachable` errors in `scripts/scanner-app/lib/watcher.js`; compare it with the current committed scanner baseline and run `npm run lint`. | Scanner release candidate                                   | A non-zero lint gate prevents release validation.                    | Reconcile from the latest committed scanner baseline (`f024f938`), which already contains the corrected control flow; do not reintroduce dead branches or suppress lint. | `npm run lint` reports 0 errors; scanner app suite passes 132/132; focused server scanner/evidence suite passes 40/40.                                                   | PROVEN |
| PM-AUD-001   | HIGH     | Full postflight unit suite          | Before 0096, `tests/partner-management-ux.test.ts` reported that service action `partner_card_job_voided` was absent from the newest `chk_partner_management_audit_action` migration constraint.            | Committed Card Job void path                                | PostgreSQL rejected its required append-only audit insert.           | Keep the forward-only 0096 strict-superset constraint migration; do not weaken the test or suppress the audit event.                                                     | Isolated PostgreSQL 17.10: all 27 prior values/rows preserved; void action accepted; unknown rejected (23514). Real wrapper proof writes exact attempted+succeeded rows. | PROVEN |
| P5-TAX-001   | HIGH     | Final payment hostile review        | Configure a correct Pack 25 Stripe Price with `tax_behavior='unspecified'`; prior validation accepted it although the account default could add VAT to the displayed inclusive amount.                      | Partner checkout before taking payment                      | Displayed and charged price could diverge.                           | Require explicit Stripe `tax_behavior='inclusive'` before checkout and before grant.                                                                                     | Mutation changing the strict check back to exclusive-only made `tests/partner-credit-purchase.test.ts` fail; restored guard passed 35/35.                                | PROVEN |
| P5-EXC-001   | HIGH     | Final payment hostile review        | Checkout Session metadata does not automatically copy to PaymentIntent/Charge; refund and dispute handlers read Charge metadata. A dispute event also contains a Dispute, not a Charge.                     | Partner refund or dispute after a completed credit purchase | Audited accounting exception could be missed.                        | Copy the server-built attribution into `payment_intent_data.metadata`; re-read a dispute's referenced Charge before recording the exception.                             | Mutating either metadata copy or Charge retrieval made the focused credit suite fail; restored path passed 35/35 and targeted hostile re-review was clear.               | PROVEN |
| SCORE-F1     | MEDIUM   | Commercial Scoreboard addendum      | Existing `/admin/growth` exposed authoritative actuals but no owner-set target store or elapsed-month pace status.                                                                                          | Super Admin Growth Command                                  | Commercial progress required external/manual target comparison.      | Add append-only/audited monthly target revisions, exact calendar-month actuals, deterministic green/amber/red/grey pace, Super Admin editor and read-only MCP.           | 86/86 Growth assertions, real PostgreSQL serialized revision/null-clear proof, 4/4 lineage rehearsal, 1440px/390px render and targeted hostile review green.             | PROVEN |
| SCN-AUTH-001 | HIGH     | Live Scanner v1.6.0 staging precheck | Complete a successful sign-in that requires MFA; the successful IPC handler called `sendHeartbeat()` while the only implementation was lexically scoped inside `app.whenReady()`, producing `ReferenceError: sendHeartbeat is not defined`. | Scanner sign-in and MFA success paths                        | A valid operator could authenticate but the Scanner could not complete its authenticated heartbeat/status refresh. | Move the unchanged canonical heartbeat implementation and its in-flight guard to module scope so startup and authentication IPC paths share the same authority; add executable tests through the actual IPC handlers. | The deliberate symbol-hiding mutation reproduced the exact ReferenceError in all three successful-auth cases; the restored implementation passes MFA, no-MFA, failed-MFA, and session-expiry recovery cases 4/4. | PROVEN |
| SCN-IDENTITY-001 | HIGH | Live Scanner v1.6.1 Shop Games auth acceptance | Authenticate the default/shared Scanner profile, which holds Shop 0 station `MV-STN-6DIISWMIEU2IKRG4`, with a Shop Games operator session. Before repair, local ACTIVE identity plus any current operator token could reach signed station authority before the exact operator/station pairing was tenant-validated. | Any enrolled Scanner profile authenticated by another tenant, including an operator-token replacement while setup or enrolment is in flight. | The wrong persisted station could be displayed and a signed station request could advance that station's nonce/auth metadata before the server's later tenant refusal. No station, credit, Card Job, reservation, capture, calibration, staging deployment or production data was mutated in this repair pass. | Pin one immutable operator-session scope across setup; validate exact station code/status before tenant reconciliation, credits, geometry or heartbeat; require a process-local validated pairing for signed requests; invalidate it on session/status changes; reject stale responses and cross-code enrolment writes. | Code commit `5d776380b478b1cee919d3381635822937982ce8`: focused identity/auth runtime 24/24; Scanner 175/175; clean full suite 6,950 passed with only two Linux/x64 golden-render cases skipped on arm64; protected Partner matrix 70/70 suites and 1,320 assertions; two independent hostile reviews CLEAR. | PROVEN |
| SCN-GEOMETRY-001 | HIGH | Live Scanner v1.5.7 STAGING physical preview | With a real card on the connected Canon LiDE 400, station `MV-STN-6DIISWMIEU2IKRG4` used acquisition region `{x:0,y:167.01,width:100,height:130}` and returned an all-white `card_not_detected` preview. A full-platen capture located the card at the raw upper-left, and an independently requested `{x:0,y:0,width:100,height:130}` crop contained the complete card. | Exact Pokémon Kings STAGING station and current calibration; production is outside scope. | FRONT/BACK physical acceptance cannot proceed, while guessed coordinates could corrupt immutable evidence provenance. | Use one incident-specific, dry-run-default STAGING command that proves exact runtime/database/station/tenant/operator/hardware/calibration identity and quiescence, appends a VALID `{0,0,100,130}` calibration, preserves the old row, repoints only the exact station, audits the transition and serializes capture arming against it. Preserve the 4 mm master floor. | Physical hashes `5b15b891…af2f6a` and `24873761…c7a8f`; 20/20 repair cases including real PostgreSQL lifecycle; focused set 74/74; protected bridge 45/45; hostile re-review CLEAR. Live GREEN preview and golden FRONT/BACK workflow remain required. | FIXED |
