# Definition of proof — mintvault-stripe-credit-purchase

| Boundary              | Evidence                                                                                                                    | Result                                                                                                                                                                 |
| --------------------- | --------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Price/quantity/tenant | Only a known package id resolves; £10 per credit and the authenticated tenant win over metadata claims.                     | Package/RBAC tests and real fulfilment prove forged million-credit/one-pence metadata still creates the catalogue's 10-credit, 10,000-pence snapshot.                  |
| Webhook authority     | Success redirect has no credit writer; paid webhook fulfilment appends the purchase.                                        | Existing Partner route/webhook branch stays authoritative; real local service fulfilment appends the immutable `stripe` row only for `payment_status='paid'`.          |
| Replay                | Sequential and concurrent duplicate delivery result in exactly one ledger row and one balance effect.                       | Eight concurrent calls return one grant, seven duplicate results, one ledger row and a 25-credit balance.                                                              |
| Cross tenant / retry  | A same-event redirect to another tenant fails closed; a failed pre-grant attempt writes no claim and can later fulfil once. | Real PostgreSQL test proves redirected same-event metadata rejects with `IDEMPOTENCY_CONFLICT`; missing-wallet attempt writes zero rows and later safely fulfils once. |
| External Stripe TEST  | A real provider API/receipt check needs explicit Stripe TEST credentials; none are available in this local-only task.       | External only; local deterministic proof complete.                                                                                                                     |
