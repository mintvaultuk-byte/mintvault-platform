# Rollback — Growth Command approved visual target

Before release, rollback is discarding the unmerged candidate branch. After a separately approved release, rollback is the repository's guarded release of the prior verified production image; no database or provider rollback is expected because this task is UI-only.
