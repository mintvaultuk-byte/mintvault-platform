# Rollout — Growth Command GB-03

1. Owner confirms the final public privacy notice/policy and authorises its publication.
2. Owner approves applying the additive `0095` migration after target-schema review and backup/rollback capture.
3. Reconcile the candidate again with current `origin/main`, run CI-equivalent gates and a targeted hostile re-review.
4. Use `scripts/safe-deploy.sh` only after live ancestry and release serialization checks pass; keep `PARTNER_APPLICATIONS_LIVE=false`.
5. Submit one unmistakably labelled non-customer TEST application through a narrowly approved controlled activation; prove durable storage, email outcome, same-request retry/refresh, no public retrieval and the live SSR/sitemap headers. Do not create a Partner account.
6. Enable `PARTNER_APPLICATIONS_LIVE` only after that proof passes.
