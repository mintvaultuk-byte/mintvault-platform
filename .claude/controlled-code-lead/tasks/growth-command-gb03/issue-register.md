# Issue register — Growth Command GB-03

| ID | Severity | Source / proof | Root cause | Repair | Class | Status |
| --- | --- | --- | --- | --- | --- | --- |
| GB03-F1 | high | `contact_inquiries` has only generic name/email/topic/message and no structured lead fields, lifecycle state or audit relationship (`shared/schema.ts:170-198`). | Reusing its free-text message would make Partner applications indistinguishable and unworkable. | Added an isolated `partner_applications` lead table and `0091` source-only migration; it has no relation to Partner tenant tables. | E/B | FIXED — deployment requires owner migration authority |
| GB03-F2 | blocker for live publication | Live `/api/config/public-flags` says `legalPagesLive:false`; the checked-in policy says `Draft for solicitor review` and has unresolved company/ICO/effective-date placeholders (`content/legal/privacy-policy.md:1-58`). | No truthful public privacy policy exists for a live PII capture form. | Do not enable or misrepresent the draft. Await owner/solicitor-approved policy and explicit migration approval before deploy. | C/E/G | OWNER_DECISION |
| GB03-F3 | medium | `/partner` and `/partner/*` are reserved for the operational portal (`client/src/App.tsx:426-430`). | A public acquisition page there would collide with auth/operations. | Added independent public `/partners`; no imports from `server/partner/*` or Partner UI. | B | FIXED |
| GB03-F4 | high | Generic contact route stores raw IP/UA and logs body-shaped responses; attribution needs PII-minimisation (`server/routes/public.ts:180-186`, `server/lib/request-logger.ts:40-80`). | Copying it would retain unnecessary identifiers and risk log leakage. | Implemented strict schema, own limiter/CSRF, bounded attribution, no IP/UA persistence, ID-only response, logger-body suppression and non-PII notification failures. | B/F | FIXED |
| GB03-F5 | blocker | Independent hostile review found both `0088_growth_partner_applications.sql` and `0088_nfc_binding_integrity.sql`; the migration runner rejects duplicate numeric prefixes. | GB-03 chose an occupied migration slot. | Renumbered source and rollback to free slot `0091`; add collision regression. | E | FIXED |
| GB03-F6 | high | A legal-page flag alone could later open `/partners` while its migration was absent; client-only noindex could not repair the SSR page. | Publication safety was coupled to an unrelated legal rollout. | Added default-false `PARTNER_APPLICATIONS_LIVE`; page, endpoint, SSR metadata and sitemap require both legal and dedicated flags. | C/E | FIXED |
| GB03-F7 | medium | New and duplicate applications returned distinguishable HTTP statuses. | The public endpoint would disclose whether a business/email pair had applied. | Both paths now return the same `201 { ok, leadId }` contract; duplicate notification is suppressed. | F | FIXED |
