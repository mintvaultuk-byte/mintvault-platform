# Independent hostile review — canonical compact grading workstation density

**Reviewer:** fresh read-only reviewer, 2026-08-14

## Verdict

No evidence-backed BLOCKER or HIGH was found in the candidate diff.

## Static verification

- One shared canonical shell remains and all five harness roles mount the real workstation.
- Desktop rail is 35%; the right pane retains the canonical internal scroll surface.
- Normal filter controls and their `setVariant` UI are removed; derivative types, URLs, and `getUrl()` remain.
- Card allocation rises to 525; certificate is bare and ratio-correct at 231×66 within a 230px visual maximum.
- Stage buttons, Grade/MVGS/centering, and panel spacing are presentation-only compacted.
- Centering thresholds are closed by default; server-issued subgrade remains visible.
- Scanner controls stay in canonical Card Details and are hidden on Grade/Review by existing CSS.
- No `shared/`, `server/`, or `migrations/` source changed; CAS/barrier/Partner-MFA source remains untouched.
- `git diff --check` passed.

## Independent browser limitation and process incident

The reviewer's existing local server returned the production application's 404 for the dev-only harness, so the reviewer did not claim independent browser-runtime proof. Before being told to avoid further external calls, the reviewer briefly started a development server; normal startup ran idempotent DB/service-tier ensure routines and was then cleanly stopped. No source, git, or deployment mutation was made. The lead's separate already-running harness supplied browser evidence.
