# Issue register — mintvault-supplies-orders

| ID     | Severity         | Status           | Finding                                                                                                                                                                                                | Closure evidence                                                                                                                                                                                                           |
| ------ | ---------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SO-001 | high             | fixed / proven   | The product lacked an approved supplies catalogue, server-priced checkout, immutable delivery/tax/order records, paid-state webhook handling, fulfilment/refund controls and Partner/Admin UI.         | Additive `0069`, server-side Stripe/session/webhook boundary, RLS/grants, Partner/Admin screens. Real PostgreSQL/HTTP/R2/browser proof in `definition-of-proof.md`.                                                        |
| SO-002 | high             | fixed / proven   | First `0069` migration preflight treated 0058's composite unique index as a named constraint, so fresh application halted even though the authoritative FK target existed.                             | Inspected the real 0058 DDL; preflight now verifies the exact unique index. Full realistic migration/rollback/reapply suites pass.                                                                                         |
| SO-003 | high             | fixed / proven   | Restricted Partner checkout needed to append `checkout_created` and use consistent catalogue/tax reads, but the initial grant/query shape either failed with 42501 or implied a broader runtime grant. | Removed `FOR SHARE`; granted append-only event INSERT only. Real restricted-runtime checkout/RLS test passes and authoritative state remains non-mutable.                                                                  |
| SO-004 | high             | fixed / proven   | Post-dispatch refund/cancel audit was written inside a transaction and then discarded when the service threw its required manual-review error.                                                         | Persist the event/audit before raising the 409; explicit manual-exception control and dashboard alert now carry it to Needs Attention. Real PostgreSQL and browser proofs pass.                                            |
| SO-005 | high             | fixed / proven   | A malformed admin `activePricePence` value was coerced to null, which could silently unprice a configurable product.                                                                                   | Strictly distinguish undefined (retain), null (intentional unprice) and invalid non-number (400). Real PostgreSQL contract test passes.                                                                                    |
| SO-006 | high             | fixed / proven   | A transient Stripe Checkout creation failure left its pending order bound to an unrecoverable idempotency key.                                                                                         | Reuse the pending order's immutable item snapshot and the same Stripe idempotency key on retry. Real PostgreSQL outage/retry proof passes without duplicate order.                                                         |
| SO-007 | high             | fixed / proven   | The completed-order action was not present in the Admin shell and a dispatched/completed payment issue could be audit-only.                                                                            | Added Shell navigation, explicit manual-review request route/control and dashboard alert; browser proves PAID→PROCESSING→DISPATCHED→COMPLETED and Needs Attention visibility.                                              |
| SO-008 | external blocker | external blocker | A real Stripe TEST checkout/refund cannot be sent without dedicated Stripe TEST credentials.                                                                                                           | No live/staging/production Stripe credential was supplied or used. Server contract, signed-webhook replay, retry, partial/full refund and no-wallet-credit behaviour are proven locally with the provider boundary double. |

All fixed items are attached to the Stage 7 ledger and `definition-of-proof.md`.
