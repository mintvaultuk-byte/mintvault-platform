# GB-04E change manifest

Status: FROZEN before implementation under the owner's durable GB-04E provider-activation approval.

## Product/test allowlist

- Add `server/fly-telemetry-service.ts`: server-only, read-only Fly Machines and Managed Prometheus adapter for the hard-allowlisted `personal/mintvault` authority.
- Modify `server/growth-intelligence-service.ts`: consume one bounded Fly snapshot for site health, capacity, infrastructure, readiness, incident and complete Growth intelligence.
- Modify `server/growth-infrastructure-intelligence.ts`: render sanitized Fly connection/machine DTOs supplied by the adapter; retain recommendation-only control.
- Modify `tests/growth-infrastructure-control.test.ts`: preserve no-mutation/no-secret assertions while accepting the explicit read-only adapter boundary.
- Add `tests/growth-fly-telemetry.test.ts`: exact endpoints/methods, parsing, thresholds, missing-data behavior, cache/stale behavior and redaction.
- Modify `tests/growth-completion-mcp-authority.test.ts` only for the capacity aggregate's asynchronous read contract.

## Governance/evidence allowlist

- This task directory and the controlled-code-lead index.
- Release evidence generated by required project scripts.

## Production configuration allowlist after candidate gates

- Create one documented Fly organisation read-only token with the shortest practical expiry.
- Set only `FLY_TELEMETRY_TOKEN` on the `mintvault` Fly app.
- Deploy only the exact verified candidate through the canonical guarded workflow.
- Revoke/unset that single token if rollback is required.

## Explicit exclusions

- No UI redesign, migration, database write, infrastructure resize/count change, autoscaling, Neon mutation, provider billing activation, payment/Partner/Scanner authority change, review outbound activation, Search Console credential creation, or MCP external connection.

## Manifest reconciliation

- Product/test files changed: 6 of hard maximum 7.
- Production modules changed: 3, including one new adapter.
- Dependencies/migrations/schema/client UI changed: 0.
- Runtime behavior changed only when `FLY_TELEMETRY_TOKEN` is present; absent configuration preserves the prior truthful fallback.
