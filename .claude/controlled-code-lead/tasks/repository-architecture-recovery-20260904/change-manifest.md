# Change manifest — architecture recovery Phase 2

> Historical boundary: this file describes Phase 2 only. The current working tree also
> contains the later owner-authorized Admin identity/session product repair. Its exact
> boundary is `admin-session-change-manifest.md` and `admin-session-file-manifest.md`;
> the “no product behavior change” statement below applies only to Phase 2.

**Committed baseline:** `09beacaa5bc03f8e80e4c7e59232df6fc466b7cb`  
**Scope:** owner-independent architecture-authority and CI-proof foundations only  
**Candidate:** checkpoint `2913bcb1092ea8f43ee1294b711a8df653a06a3d` plus reviewed eight-file executable/dependency diff SHA-256 `776721dfcd7cca0196bea0b1e5eedaa98d53efa44a76ebec622ea06c45e9c8f6`; still a local dirty working tree, not an immutable candidate, pushed, deployed, or released

## Executable changes

- Add seven strict component-readiness manifests, one literal canonical index, and a
  runtime compiler that derives the existing readiness arrays/signals from that index.
- Replace hand-maintained readiness lists in `server/readiness.ts` with projections from
  the compiled registry while preserving their values and order.
- Add an AST architecture gate and reviewed snapshot covering root-reachable HTTP
  composition/order, route-local middleware order, nested client leaf/guard dominance,
  lexical import/alias authority, component ownership, tables and SQL effects,
  object/provider origin and callsite effects, jobs/timers, migration identities/lineage,
  layer exceptions, and role/session/pricing authorities.
- Add exact legacy topology keys. There is no directory or category catch-all; any new
  record without a declared owner or exact key fails.
- Wire architecture, migration-reference, script-syntax, TypeScript debt, isolated
  Partner, and Scanner proof commands into the blocking hosted `check` job with explicit
  Node 20 server/build and Node 22 Scanner phases.
- Declare the Scanner tests' existing `happy-dom` import in the nested Scanner manifest
  and lock exact version 20.11.1 as a development-only package, preventing the critical
  runner from borrowing the root workspace installation.
- Add fail-closed Partner/Scanner non-vacuity floors, Scanner nested-dependency isolation,
  CI phase-order validation, migration-reference classification, and exact tracked-file
  contracts for the three TypeScript ratchets and JavaScript syntax inventory.

## Proof changes

- Add mutation tests for unowned topology, unreachable and reordered registrars, nested
  client guards, component index/runtime/source-root drift, disabled effects, SQL helper
  and CTE behavior, route-local provider effects, migration leading-zero collisions and
  lineage, stale layer exceptions, semantic authority drift, CI conditional/order
  bypasses, non-vacuity floors, no-check movement, dotted migrations, and zero-file
  syntax baselines. The JavaScript syntax baseline binds 65 tracked modules, including
  direct children of `scripts/` and nested Scanner/watcher scripts.
- Record the generated 8,608-record topology, its 3,673 exact legacy keys, and exact current diagnostic inventories:
  345 test diagnostics, 11 script diagnostics, and 5 architecture-scope diagnostics.
  These are debt ceilings, not correctness waivers; new fingerprints and new
  `@ts-nocheck` files fail.
- Bind the final local hostile recheck to checkpoint `2913bcb1`. The server, client, and
  data/runtime lanes all returned CLEAN after the accepted false-green mutations were
  repaired. The data/runtime lane then independently verified the nested Scanner
  dependency and found that the existing macOS copy step would include development
  modules; packaging remains explicitly outside this Phase 2 claim.

## Governance and documentation changes

- Correct `CLAUDE.md` to point to executable multi-authority topology rather than a
  singular storage/route claim.
- Update the architecture authority guide, repair graph evidence, issue/proof ledgers,
  task ledger, review status, rollback packet, and repository task index.
- The exact Phase 2 path/digest boundary is recorded in `phase-2-file-manifest.md` after
  the independent review loop completes.

## Explicitly unchanged

- No product route, auth, grading, payment, credit, certificate, tenant-isolation,
  object-write, provider, or pricing behavior was intentionally changed.
- No schema or migration SQL was authored, edited, or applied.
- No production dependency was changed. The only dependency change is the owner-approved
  nested Scanner `happy-dom` development dependency and lockfile closure used by two
  behavioral test files. No package was built or distributed, and no claim is made that
  the current macOS packager excludes development modules.
- No secrets, file permissions, database/provider/object-store state, staging,
  production, GitHub settings, Scanner devices, deployment, push, publish, or release
  state was changed.
- Pre-existing White Ace test WIP and `.gitleaksignore` remain preserved and outside the
  Phase 2 implementation boundary.
