# Reviewer status — Canonical grading left-rail refinement

No subagents were used. This is a small, single-component owner-directed presentation repair, and the Lead directly traced the sole component mount, normal-flow rail contract, revision barrier and the existing production-component harness.

| Scope | Status | Evidence |
|---|---|---|
| Presentation source | accepted | `CertificatePreviewPanel.tsx` is the sole removable chrome surface and now contains only the bare image/compact-state presentation. |
| Shared-role topology | accepted | `GradingWorkstation.tsx` mounts the component exactly once; architecture tests enforce one production implementation. |
| Height allocation | accepted | `WorkstationPreviewAside.tsx` places card first in a `flex-1` child and preview second in `shrink-0`, so no parent layout change is required. |
| Functional authority | preserved and tested | Fetch/revision acknowledgement/abort flow remains intact; retry is covered without weakening stale/revision tests. |
| Browser acceptance | accepted locally | Five roles at 1280×800 and 1024×768: card height +54.9px, 266 × 76px undistorted certificate, no residual chrome/text or horizontal overflow. |
