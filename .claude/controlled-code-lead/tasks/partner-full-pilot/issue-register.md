# Issue register — partner-full-pilot

| ID | Severity | Source / reproduction | Root cause | Repair | Proof | Status |
| --- | --- | --- | --- | --- | --- | --- |
| PFP-01 | HIGH | Staging Super Admin request `GET /api/admin/submissions/327/certs` returned 500 for a Partner-imported two-card destination. | Legacy picker queried removed `cert.cert_id` and joined only legacy `cards`; Partner imports link certificates through `submission_items`. | A Partner-owned, read-only adapter uses `certificate_number` and supports both canonical link paths; the admin picker calls it. | On staged `6f4e2652`, the authenticated picker returns 200 with both certificates. | PROVEN |
| PFP-02 | HIGH | Staging connector detail labelled the pilot's two valid per-card reservations as `reservation_link_inconsistent`. | The read-only projection retained obsolete single-reservation cardinality logic. | Return all reservations plus count/active count; UI displays the list. | On staged `6f4e2652`, detail returns two reservations, two active before settlement, no link conflict and no reconciliation issue; after settlement the same two reservations are consumed (zero active) without conflict. | PROVEN |
| PFP-03 | HIGH | In the staging two-card pilot, the final HQ approval returned PostgreSQL `P0001`; both work items were approved but the wallet remained available 98 / reserved 2 / consumed 0. | The real connector importer created a Partner destination as `draft`; the intact status-transition guard correctly rejects the later `draft → ready_to_return` settlement transition. | Create completed Partner intake destinations directly as `in_grading`; preserve the guard. Recover the deterministic staging item only through the normal audited Super Admin transition route. | Exact-SHA CI green; live authenticated normal status recovery reached `ready_to_return` and the existing idempotent settlement transaction produced available 98 / reserved 0 / consumed 2, with no manual balance edit. | PROVEN |

No speculative findings were added. The unsigned Stripe supply webhook proof remains an external staging configuration dependency, not a code finding.
