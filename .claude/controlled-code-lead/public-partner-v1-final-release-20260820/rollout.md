# Rollout — Public Partner Network v1 final release

This is a gated runbook, not authority to execute it.

1. Obtain an owner decision explicitly approving current-main Growth `0101` and public presence `0102`, expressly excluding Google `0103`.
2. At the target, capture journal/schema/flag/health evidence and a verified recovery point. Reconcile the disposable-test-Partner manifest; never physically delete protected Partner history.
3. Run the authorised staging deployment and anonymous public route/API/SSR/sitemap checks with only safe negative references.
4. Repeat the target-time preflight immediately before production deployment. Apply only the owner-approved migrations, deploy the exact reviewed SHA, and keep the directory flag off until Super Admin step-up activates it with a reason.
5. Record anonymous/browser acceptance and containment proof. Any real Partner onboarding requires separately supplied details and action-time owner authorisation.
